import 'package:flutter/material.dart';

class ClientWelcome extends StatelessWidget {
  const ClientWelcome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Welcome Client'),
    );
  }
}
